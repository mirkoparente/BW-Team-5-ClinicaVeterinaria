@model BW_Team_5_ClinicaVeterinaria.Models.Visite

@{
    ViewBag.Title = "Edit";
}

<h2>Modifica visita</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    @Html.Partial("_CreateEditVisita")


    @Html.HiddenFor(model => model.IdPaziente, new { htmlAttributes = new { @class = "form-control" } })
    @Html.HiddenFor(model => model.IdVisite, new { htmlAttributes = new { @class = "form-control" } })

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Modifica" class="btn btn-default" />
        </div>
    </div>

}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
